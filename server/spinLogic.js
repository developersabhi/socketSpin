const fs = require('fs');

const wheel =["A","B" ,"C","D","E","F"];

function spin(){
    const randomIndex = Math.floor(Math.random() * Wheel.length);
    return wheel[randomIndex];
}

function result(){
    
}